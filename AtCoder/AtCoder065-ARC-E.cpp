#include<bits/stdc++.h>
using namespace::std;

const int N = 100000 + 5;

int n;
int a;
int b;
int x[N];
int y[N];

long long solve(){
	
}

int main(){
	scanf("%d %d %d", &n, &a, &b);
	for(int i = 0; i < n; i++) scanf("%d %d", x + i, y + i);
	printf("%lld\n", solve());
	return 0;
}
