#include<bits/stdc++.h>
using namespace::std;

int n;
long long a[N];

int main(){
	scanf("%d", &n);
	for(int i = 1; i <= n; i++) scanf("%lld", a + i);
	while(*max_element(a + 1, a + n + 1) >= n){
		long long 
		for(int i = 1; i <= n; i++){
			
		}
	}
	return 0;
}
